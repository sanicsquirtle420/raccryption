#ifndef RACTILS_HPP
#define RACTILS_HPP
#include <vector>
#include <string>

std::vector<std::string> split_str(std::string str, std::string delmiter) ;
std::string file_name(std::string path, int type) ;

#endif 